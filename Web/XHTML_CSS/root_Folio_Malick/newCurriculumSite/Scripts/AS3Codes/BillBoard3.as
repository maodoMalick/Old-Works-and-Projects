package Scripts.AS3Codes {		// ******** HOW IT WORKS ****************	// There is a series of classes BillBoard1,2,3 with different transitions	// that will be loaded in "XmlPictureLoader" class to mask some pictures loaded via an XML file.	// The "XmlPictureLoader" in turn will be loaded inside the class "NewMediaCenter08",	// which in turn will be loaded in the main FLA file.	import flash.display.MovieClip;	import flash.display.Sprite;	import flash.display.Shape;	import flash.utils.Timer;	import flash.events.TimerEvent;	public class BillBoard3 extends MovieClip {		// Create main container		private var _billBoard:Sprite;		private var Mask:Shape;		private var c_PosY:Number = 0;		private var c_PosX:Number = 0;		private var c_PosY1:Number = 27;		private var c_PosX1:Number = 94;		private var c_PosY2:Number = 54;		private var c_PosX2:Number = 47;		private var c_PosY3:Number = 27;		private var c_PosX3:Number = 0;		// TIMER		private var maskTimer:Timer = new Timer(50);		public function BillBoard3() {			// Instantiate main container			_billBoard = new Sprite;			addChild(_billBoard);			// this was for the purpose of testing in another movie.			//city_mc.mask = _billBoard;			maskTimer.addEventListener(TimerEvent.TIMER, showImage);			maskTimer.start();		}				private function showImage(myEvent:TimerEvent):void {			Mask = new Shape();			Mask.graphics.beginFill(0x000000, .5);			Mask.graphics.drawRoundRect(c_PosX, c_PosY, 46, 26, 10);			Mask.graphics.endFill();			_billBoard.addChild(Mask);			c_PosX += 47;			if (c_PosX > 94) {				c_PosX = 94;				maskTimer.removeEventListener(TimerEvent.TIMER, showImage);				maskTimer.addEventListener(TimerEvent.TIMER, showImage1);				maskTimer.start();			}		}				function showImage1(myEvent:TimerEvent):void {			Mask = new Shape();			Mask.graphics.beginFill(0x000000, .5);			Mask.graphics.drawRoundRect(c_PosX1, c_PosY1, 46, 26, 10);			Mask.graphics.endFill();			_billBoard.addChild(Mask);			c_PosY1 += 27;			if (c_PosY1 > 54) {				c_PosY1 = 54;				maskTimer.removeEventListener(TimerEvent.TIMER, showImage1);				maskTimer.addEventListener(TimerEvent.TIMER, showImage2);				maskTimer.start();			}		}				function showImage2(myEvent:TimerEvent):void {			Mask = new Shape();			Mask.graphics.beginFill(0x000000, .5);			Mask.graphics.drawRoundRect(c_PosX2, c_PosY2, 46, 26, 10);			Mask.graphics.endFill();			_billBoard.addChild(Mask);			c_PosX2 -= 47;			if (c_PosX2 < 0) {				c_PosX2 = 0;				maskTimer.removeEventListener(TimerEvent.TIMER, showImage2);				maskTimer.addEventListener(TimerEvent.TIMER, showImage3);				maskTimer.start();			}		}				function showImage3(myEvent:TimerEvent):void {			Mask = new Shape();			Mask.graphics.beginFill(0x000000, .5);			Mask.graphics.drawRoundRect(c_PosX3, c_PosY3, 46, 26, 10);			Mask.graphics.endFill();			_billBoard.addChild(Mask);			c_PosX3 += 47;			if (c_PosX3 > 47) {				c_PosX3 = 47;				maskTimer.removeEventListener(TimerEvent.TIMER, showImage3);			}		}	}}